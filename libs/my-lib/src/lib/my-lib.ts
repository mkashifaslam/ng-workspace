export function myLib(operand1: number, operand2: number): string {
  return `Sum of ${operand1} and ${operand2} is ${operand1 + operand2}`;
}

export function alertMessage(message: string) {
  return `🎉🎉🎉 ${message} 🥳🥳🥳`;
}
